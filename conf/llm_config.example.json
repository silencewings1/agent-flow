{
  "_comment": "每节点 LLM 配置示例。复制为 llm_config.json 并按需修改。API Key 不写这里，改为设置对应环境变量（见 api_key_env）。最终配置 = provider默认 ← defaults ← nodes[名称]。",

  "defaults": {
    "provider": "anthropic",
    "temperature": 0.3,
    "max_tokens": 2048
  },

  "nodes": {
    "planner": {
      "provider": "anthropic",
      "model": "claude-opus-4-8",
      "system": "你是资深需求分析师，把需求拆成清晰的子任务。",
      "temperature": 0.2
    },
    "coder": {
      "provider": "openai",
      "model": "gpt-4o",
      "system": "你是高级工程师，只输出可运行代码。",
      "api_key_env": "OPENAI_API_KEY"
    },
    "debugger": {
      "provider": "anthropic",
      "model": "claude-sonnet-4-6"
    },
    "reviewer": {
      "provider": "mock",
      "_note": "评审内容用 mock，真实决定仍由人在回路给出"
    }
  }
}
